import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const app = fs.readFileSync(path.join(root, "public/app.js"), "utf8");
const html = fs.readFileSync(path.join(root, "public/index.html"), "utf8");
const css = fs.readFileSync(path.join(root, "public/styles.css"), "utf8");
const ultimateData = fs.readFileSync(path.join(root, "public/ultimate-card-data.js"), "utf8");
const mobileJs = fs.readFileSync(path.join(root, "public/mobile-game.js"), "utf8");
const mobileCss = fs.readFileSync(path.join(root, "public/mobile-game.css"), "utf8");

assert.match(html, /id="ultimateModeButton"[\s\S]*data-required-role="pro_plus"/);
assert.match(html, /ultimate\/ultimate-home\.jpg/);
assert.match(html, /Tennis Courts Academy · <span>V5\.25<\/span>/);
assert.match(html, /ultimate-card-data\.js\?v=5\.25\.0/);
assert.match(html, /data-ultimate-ai="legend"/);
assert.match(html, /id="ultimatePlayerChoices"[\s\S]*ultimate\/conti\/lobby\.png[\s\S]*ultimate\/brentwood\/lobby\.png/);
assert.match(html, /Draft 1 sur 3/);
assert.match(html, /Énergie 💡/);
assert.match(html, /deux emplacements de réserve/i);
assert.match(app, /ULTIMATE_STARTING_ENERGY = 3/);
assert.match(app, /function canAccessUltimateFeatures\(\)[\s\S]*\["pro_plus", "admin"\]/);
assert.match(app, /ULTIMATE_DECK_SIZE = 48/);
assert.match(app, /window\.ULTIMATE_CARD_DATA/);
assert.match(app, /state\.setMatch\.enabled = true/);
assert.match(app, /newGame\(\{ preserveSet: true, resetUltimate: true \}\)/);
assert.match(app, /!resetUltimate && state\.players\.length === 2/);
assert.match(app, /renderUltimateReserveInHand/);
assert.match(app, /card-hover-panel ultimate-reserve-hover-panel/);
assert.match(app, /data-boost-reserve=.*disabled/);
assert.doesNotMatch(app, /ultimate-profile-avatar/);
assert.match(ultimateData, /window\.ULTIMATE_CARD_DATA/);
assert.equal((ultimateData.match(/"player": "Conti"/g) || []).length, 48);
assert.equal((ultimateData.match(/"player": "Brentwood"/g) || []).length, 48);
assert.match(app, /playerOrder: \[0, 1\]/);
assert.match(app, /ULTIMATE_MODE\.playerOrder = characterIndex === 0 \? \[0, 1\] : \[1, 0\]/);
assert.match(app, /length: 36/);
assert.match(app, /ultimate\/conti\/lobby\.png/);
assert.match(app, /ultimate\/brentwood\/lobby\.png/);
assert.match(app, /ultimateCardBackForPlayer/);
assert.match(app, /assets\/ultimate\/conti\/back\.png/);
assert.match(app, /assets\/ultimate\/brentwood\/back\.png/);
assert.match(app, /characterStarActive \? ultimateCharacter\.power : ultimateCharacter\.character/);
assert.match(app, /visibleStars < 2/);
assert.match(app, /beginUltimatePostExchange\(winner\)/);
assert.match(app, /perdant de l’échange, récupère les.*premières cartes/);
assert.match(app, /Draft du vainqueur/);
assert.match(app, /window\.confirm\(`Dépenser 1 ÉNERGIE/);
assert.match(app, /ultimateDecks: \[\[\], \[\]\]/);
assert.match(app, /player\.endurance = STARTING_ENDURANCE/);
assert.match(app, /card\._fromReserve = true/);
assert.match(app, /beginUltimateDraft\(0, 0, "set-start"\)/);
assert.match(app, /openUltimateHandDiscardChoice/);
assert.match(app, /openUltimateReserveTrimChoice/);
assert.match(css, /ultimate-reserve-mark/);
assert.match(css, /ultimate-reserve-actions[\s\S]*pointer-events: auto/);
assert.match(app, /player\.reserve \|\| \[\]\)\.some\(\(candidate\) => candidate\.uid !== card\.uid\)/);
assert.match(app, /const choices = \[\.\.\.player\.hand\.filter[\s\S]*\.\.\.reserveChoices, \.\.\.discardChoices\]/);
assert.match(app, /if \(ULTIMATE_MODE\.active\) ULTIMATE_MODE\.postExchange = null/);
assert.match(app, /replayingCompletedUltimateMatch/);
assert.doesNotMatch(app, /data-use-reserve=.*>JOUER<\/button>/);
assert.match(app, /Côté faible\.\*limité à REVERS, LOB ou REMISE/);
assert.match(app, /Votre prochain coup coûte 1 endurance de moins/);
assert.match(app, /ultimate-profile-energy/);
assert.match(app, /openUltimateEnergyChoice/);
assert.match(app, /maybeSpendUltimateEnergyForAI/);
assert.match(app, /openUltimateCharacterState/);
assert.match(app, /Pouvoir étoile déclenché/);
assert.match(app, /ultimateOtherShotsEndPower/);
assert.match(app, /ultimateWeakestCopiesStrongest/);
assert.match(app, /ultimateBoostFromDiscard/);
assert.match(css, /character-zone:has\(\.ultimate-profile-discard\)/);
assert.match(app, /data-start-ultimate-post-exchange/);
const finishGameBody = app.slice(app.indexOf("function finishGame("), app.indexOf("function completeOnePointTournamentMatch("));
assert.doesNotMatch(finishGameBody, /setTimeout[\s\S]*beginUltimatePostExchange/, "la fin Ultimate doit attendre le clic du joueur");
const playCardBody = app.slice(app.indexOf("function playCard("), app.indexOf("function completePlayedCardResolution("));
assert.ok(playCardBody.indexOf("player.endurance -= cost") < playCardBody.indexOf("applyEffect(playerIndex, playedCard)"), "Le coût doit être payé avant la résolution de l'effet");
assert.ok(playCardBody.indexOf("let sacrificedCard = null") < playCardBody.indexOf("player.endurance -= cost"), "Le sacrifice du BOOST doit être validé avant tout paiement");
assert.match(playCardBody, /BOOST annulé[\s\S]*state\.pendingBoost = null[\s\S]*render\(\);[\s\S]*return;/);
assert.match(app, /function secureUltimateTurnContinuation/);
assert.match(app, /state\.activePlayer = opponentIndex;\s*secureUltimateTurnContinuation\(playerIndex\);/);
assert.match(app, /ultimateMode: ULTIMATE_MODE\.active/);
assert.match(app, /const restoredUltimate = snapshot\.ultimateMode/);
assert.match(app, /forceSoloBlockedExchangeLoss\(SOLO_AI\.playerIndex\)/);
assert.match(app, /Number\(card\.ultimateExchangeNumber\) === exchangeNumber/);
assert.match(playCardBody, /ultimateExchangeNumber: ULTIMATE_MODE\.active/);
assert.match(html, /id="ultimateExportLogsButton"/);
assert.match(app, /function auditUltimateRuntime/);
assert.match(app, /function analyzeUltimateDiagnostic/);
assert.match(app, /function soloLegalInventoryLog/);
assert.match(app, /function shouldSoloClosePreparedPlacement/);
assert.match(app, /ultimate_reserve_added/);
assert.match(app, /ultimate_exchange_started/);
assert.match(app, /function startNextUltimateExchange\(/);
assert.match(app, /function ultimatePlayedCardsForDiscard/);
assert.match(app, /player\.freeBoostNext \|\| colorBoost/);
assert.match(app, /captureTurnSnapshot\(\);\s*render\(\);\s*beginUltimateServiceReveal\(\);/);
assert.match(app, /player\.played = \[\]/);
assert.match(app, /function ensureUltimateNextExchangeStarted/);
assert.match(app, /window\.setTimeout\(\(\) => ensureUltimateNextExchangeStarted\(completedExchangeNumber\), 350\)/);
assert.match(app, /flow\.distributionStarted = true/);
assert.match(app, /flow\.phase = "winner-draft"/);
assert.match(app, /if \(!flow\.phase\) flow\.phase = "reserve"/);
assert.match(app, /choix de réserve non bloquant/);
assert.doesNotMatch(app, /function startNextUltimateExchange\([^)]*\) \{\s*if \(!ULTIMATE_MODE\.active \|\| !state\.gameOver/);
assert.match(app, /if \(!ULTIMATE_MODE\.active && SERVER_SYNC\.enabled/);
assert.match(app, /function openUltimateMarkChoice/);
assert.match(app, /data-mark-card/);
assert.match(app, /markedForDiscard = true/);
assert.match(css, /ultimate-discard-mark/);
assert.match(css, /@media \(max-width: 860px\)[\s\S]*\.ultimate-resources/);
assert.doesNotMatch(mobileJs, /ULTIMATE_MODE|ultimate-resources|ultimateModeButton/);
assert.doesNotMatch(mobileCss, /ultimate-resources|lobby-mode-ultimate/);

for (const player of ["conti", "brentwood"]) {
  const folder = path.join(root, "public/assets/ultimate", player);
  const cards = fs.readdirSync(folder).filter((name) => /^card-\d{2}\.png$/.test(name));
  assert.equal(cards.length, 36, `${player} doit disposer de 36 cartes COUP`);
  for (const required of ["back.png", "character.png", "power.png"]) {
    assert.ok(fs.existsSync(path.join(folder, required)), `${player}/${required} est requis`);
  }
  assert.ok(fs.existsSync(path.join(folder, "lobby.png")), `${player}/lobby.png est requis`);
}
const effectsFolder = path.join(root, "public/assets/ultimate/effects");
assert.equal(fs.readdirSync(effectsFolder).filter((name) => /^card-\d{2}\.png$/.test(name)).length, 12);
assert.doesNotMatch(app, /ultimatePlaceholderEffect/);

console.log("V5 Ultimate admin, rules, resources and mobile isolation checks passed.");
